repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.11
    hooks:
      # Запускаем линтер
      - id: ruff-check
        args: [--fix]
      # Запускаем форматер
      - id: ruff-format
